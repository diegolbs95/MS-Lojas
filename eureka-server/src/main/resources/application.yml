spring:
  application:
    name: api-eureka-server
server:
  port: 8761

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:${server.port}/eureka/




#spring.application.name= api-eureka-server
#server.port= 8761
#
#eureka.client.register-with-eureka= false
#eureka.client.fetch-registry=false
#eureka.client.service-url: http://localhost:${server.port}//eureka/
